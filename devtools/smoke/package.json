{
  "name": "nana-smoke",
  "private": true,
  "description": "Dev-only smoke tests for settlement workflow (not CI)",
  "scripts": {
    "smoke:settlement": "node playwright-test-settlement-scenario-smoke.js",
    "smoke:settlement:legacy": "node playwright-test-settlement-preview-legacy.js",
    "smoke:settlement:all": "npm run smoke:settlement:legacy && npm run smoke:settlement",
    "smoke:queue": "node playwright-test-queue-settlement-smoke.js",
    "smoke:draft": "node playwright-test-draft-settlement-smoke.js",
    "smoke:all": "npm run smoke:settlement:all && npm run smoke:queue && npm run smoke:draft",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}
